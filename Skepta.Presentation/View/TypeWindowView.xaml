<UserControl x:Class="Skepta.Presentation.View.TypeWindowView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Skepta.Presentation.View"
             mc:Ignorable="d" 
             d:DesignHeight="1080" d:DesignWidth="1920">
    <Grid>
        <Label Height="120"  Content="{Binding Story}" Background="LightGray" VerticalAlignment="Top"/>
        <TextBox Height="200"  Text="{Binding TypedText}" Background="Red" VerticalAlignment="Bottom"/>
        <Button Width="70" Height="30" Command="{Binding SendCommand}" VerticalAlignment="Center" HorizontalAlignment="Left" Content="Verstuur"/>
    </Grid>
</UserControl>
