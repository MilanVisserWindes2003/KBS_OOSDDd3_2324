<UserControl x:Class="Skepta.Presentation.View.TextToSpeechView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Skepta.Presentation.View"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <StackPanel Orientation="Vertical">
            <Label Content="{Binding Text}" VerticalAlignment="Center"/>
            <Button Content="Play" Command="{Binding Speak}"/>
            <ComboBox HorizontalAlignment="Left" Margin="26,22,0,0" VerticalAlignment="Top" Width="120" 
                ItemsSource="{Binding SpeedOptions}"
                SelectedItem="{Binding SelectedSpeedOption}">
            </ComboBox>
        </StackPanel>
    </Grid>
</UserControl>
